class Solution {
    public int searchInsert(int[] nums, int target) {
        int x;
        for( x=0;x<nums.length;x++)
        {
            if(nums[x] >= target) return x;
        }
        return x;
    }
}
