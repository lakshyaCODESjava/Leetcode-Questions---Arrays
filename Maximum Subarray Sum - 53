class Solution {
    public int maxSubArray(int[] nums) {
        int  max = Integer.MIN_VALUE;
        int sum = 0;
        for(int x=0;x<nums.length;x++)
        {
            sum+=nums[x];
            if(sum>max) max = sum;
            if(sum<0) sum=0;
        }
        return max;
    }
}
